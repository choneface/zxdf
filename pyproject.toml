[project]
name = "zxdf"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "choneface", email = "gavin.garcia0001@gmail.com" }
]
requires-python = ">=3.9"
dependencies = [
    "platformdirs>=4.4.0",
    "typer>=0.21.1",
]

[project.scripts]
zxdf = "zxdf.main:app"

[build-system]
requires = ["uv_build>=0.9.28,<0.10.0"]
build-backend = "uv_build"
